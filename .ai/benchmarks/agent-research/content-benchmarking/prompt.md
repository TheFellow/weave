Explain why query latency alone is insufficient to evaluate Weave as an agent
research tool. Describe what the initial uncontrolled dogfood recorded where
the tool lost the agent, how benchmark friction turns into product behavior,
how the paired with-Weave and without-Weave arms hold the question and evidence
standard constant while isolating the tool, which artifacts and measurements
are retained, and how the first paired result should and should not be read.
